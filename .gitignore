# Keep local-only secret overrides out of git.
config.local.js
node_modules/
dist-pages/
.DS_Store

# Superpowers brainstorming visual companion (local scratch, not for commit)
.superpowers/

# Local dev tooling config and working notes (not part of the shipped product)
.claude/
docs/superpowers/
CLAUDE_HANDOVER.md
