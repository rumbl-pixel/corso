{
  "name": "runclub-platform",
  "version": "1.1.0",
  "description": "School run club platform (Marathon Kids + StrideTrack inspired) — barcode lap tracking, kiosk, awards, reports.",
  "scripts": {
    "build": "node scripts/build.js",
    "deploy": "node scripts/build.js --deploy",
    "test": "node tests/portal-smoke.test.js",
    "serve": "python3 -m http.server 8080"
  },
  "license": "UNLICENSED",
  "private": true
}
